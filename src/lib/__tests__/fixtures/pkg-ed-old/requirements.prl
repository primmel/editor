// The editioned package's requirements — the OLD edition (the diff base).
requirement /req/ed/stability {
  name "Stability"
  statement "The instrument SHALL remain stable."
  binds_to { model.parameters.zero }
  source { doc "urn:example:ed:2025" clause "5.1" }
}
